extends ../layout.pug

block content
  h1= "ActiveCollab Light"

  if projects

    ul.projects
      each project in projects
        li
          a(href="/projects/" + project.id)= project.name

  else

    p="Projecten worden geladen..."
